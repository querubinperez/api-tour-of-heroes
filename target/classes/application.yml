# Server properties
server:
  port: 8080

# Spring properties
spring:
  # Dev is active by default. This value can be overwritten by java arg param 
  profiles:
    active: heroku
  application:
    name: tourofheroes-api